import 'package:flutter/material.dart';

class Kits extends StatelessWidget {
  const Kits({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("kits"),
    );
  }
}
